/*
  Warnings:

  - You are about to drop the column `waktu_transaksi` on the `transaksi` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `transaksi` DROP COLUMN `waktu_transaksi`;
